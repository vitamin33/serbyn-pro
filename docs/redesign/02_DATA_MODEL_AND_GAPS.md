# 02 — Data Model & Gaps (design → real data)

> Inventory verified 2026-06-20 across serbyn-pro + Ascend. Legend: **HAVE** = use as-is · **DERIVE** = transform existing · **NEW** = create.

## 1. Hero metric strip + availability (home)

Design: 4 mono metrics + `AVAILABLE FOR Q3 ENGAGEMENTS` badge.

| Cell | Real source | Status |
|---|---|---|
| `30 agents` (mature N) | Ascend `.sprint-state.json::agent_maturity` / `trust.db` | HAVE |
| `1,375 tests` | `.sprint-state.json` (`tests_baseline` 1,178; collected 1,375) | HAVE |
| `L0–L4 trust` / `12 at L4` | `trust.db` agents table | HAVE |
| 4th cell (`93% success` or `500h saved`) | `audit.jsonl` success rate / `value_log.md` | HAVE (derive number) |
| availability badge | — | **NEW** (1 field in `metrics.json`) |

→ **NEW `data/site/metrics.json`** (Ascend-generated, committed to repo, read at build). Schema in §5.

## 2. Work index + featured cards

Design per-card: system-id, category, status chips, title, description, tech tags, impact score, cover; plus sidebar category counts, filter tabs, sort (Relevance/Impact/Recent).

| Element | Real source | Status | Action |
|---|---|---|---|
| title / description / summary | case-study frontmatter | HAVE | — |
| `category` | frontmatter enum | DERIVE | realign enum (§4) |
| `impact_score` (89–96) | frontmatter | HAVE | render as badge |
| `tech[]` | frontmatter | HAVE | render as tags |
| `highlight_metrics[]` | frontmatter | HAVE | source for chips |
| status chips (`95% RELIABLE`, `ZERO-TRUST`, `THROUGHPUT`) | — | **DERIVE/NEW** | add `status_chips?: {label,variant}[]`; seed from highlight_metrics |
| `system_id` (`01_ARCH_001`) | — | **DERIVE** | compute `{NN}_{CAT}_{NNN}` from category+order, or add optional field |
| `cover` image | frontmatter `cover` (empty) | **NEW** | generate branded OG-card per slug (next/og) → `public/covers/<slug>.png`, or real screenshot |
| sort: Impact / Recent | impact_score / date | HAVE | — |
| sort: Relevance | — | **NEW** | add optional `relevance?: number` (default = impact_score) |
| sidebar counts | count by category | DERIVE | compute at build or from metrics.json |

**Schema extension — `CaseStudyMeta` (additive, all optional → no breakage):**
```ts
system_id?: string;                 // "ARCH_001"; rendered "01_ARCH_001" by index
status_chips?: { label: string; variant: 'active'|'success'|'warning'|'neutral' }[];
relevance?: number;                 // sort weight; default impact_score
cover?: string;                     // already exists; populate
```

## 3. Article layout

Design: numbered essay-map TOC (`01_SUMMARY`…`06_CONCLUSION`), read time, cover, sticky code panel, author, date.

| Element | Real source | Status | Action |
|---|---|---|---|
| body / readingTime / date / author | blog MDX | HAVE | — |
| essay-map TOC | MDX `##` headings | **DERIVE** | parse H2s (rehype-slug already wired) → numbered TOC + IntersectionObserver active state |
| code panel | first fenced ```` ```lang ```` block | **DERIVE** | extract featured code block; optional `featured_code?: {lang,code,label}` frontmatter override |
| cover | — | **NEW** | reuse the dynamic OG card already built (`opengraph-image.tsx`) as the on-page hero too |

**Schema extension — `BlogPostMeta` (additive):**
```ts
featured_code?: { lang: string; label?: string; code: string };  // optional code-panel override
cover?: string;                                                  // optional; else generated
toc?: boolean;                                                   // default true; show essay-map
```

## 4. Category taxonomy alignment

Design sidebar: **Architecture · Infrastructure · Optimization · Research**.
Current `CaseStudyMeta.category`: `platform | infrastructure | optimization | feature`.
Current `BlogPostMeta.category`: `architecture | engineering | operations | strategy`.

→ **Unify a public-facing taxonomy** for the sidebar/filters: `architecture | infrastructure | optimization | research`. Implement as a **display mapping** (don't break existing enums):
```
case study: platform→Architecture, infrastructure→Infrastructure, optimization→Optimization, feature→Architecture
blog:       architecture→Architecture, engineering→Infrastructure, operations→Optimization, strategy→Research
```
Add a `lib/taxonomy.ts` with the map + counts helper. (Optionally widen the enums later; mapping is zero-risk now.)

## 5. NEW `data/site/metrics.json` (the rollup)

Generated by Ascend (`scripts/site_metrics_rollup.py`, see `03_PUBLISHING_PIPELINE.md §4`), committed to serbyn-pro, read at build in `lib/site-metrics.ts`.

```json
{
  "generated_at": "2026-06-20T00:00:00Z",
  "availability": { "open": true, "label": "AVAILABLE FOR Q3 ENGAGEMENTS" },
  "hero_metrics": [
    { "label": "AGENTS", "value": "30", "sub": "20 mature" },
    { "label": "TESTS", "value": "1,375", "sub": "ruff+mypy clean" },
    { "label": "TRUST", "value": "L0–L4", "sub": "12 at L4" },
    { "label": "AUTOMATED", "value": "500h+", "sub": "saved YTD" }
  ],
  "category_counts": { "architecture": 2, "infrastructure": 1, "optimization": 0, "research": 0 },
  "system_status": "ONLINE"
}
```
Source map: agents/mature ← `.sprint-state.json`/`trust.db`; tests ← sprint-state; trust ← trust.db; automated ← `value_log.md` aggregate; counts ← case studies; availability ← a hand-set field in an Ascend config.

## 6. Assets / covers (NEW)

- **Covers:** default = generated branded card (reuse `next/og` card style) written to `public/covers/<slug>.png` at build, OR served live via a `/work/[slug]/cover` route. No design dependency on real screenshots; swap later.
- **Blog thumbnails:** same generator, smaller; or reuse the post's OG image.
- `public/` today has only logo/favicon/og — no project imagery. The generator removes that blocker.

## 7. Gap summary (what to actually build)

1. `data/site/metrics.json` + Ascend rollup generator + `lib/site-metrics.ts` reader. **NEW**
2. `CaseStudyMeta` + `BlogPostMeta` additive fields (system_id, status_chips, relevance, featured_code, cover). **NEW (schema)**
3. `lib/taxonomy.ts` display mapping + counts. **NEW**
4. Cover/thumbnail generator (next/og). **NEW**
5. Essay-map TOC + code-panel extraction from MDX. **DERIVE**
6. RSS feed, `/api/subscribe` (Resend), pagination. **NEW** (see `03`).
7. Backfill `status_chips`/`system_id`/`cover` into the 3 existing case-study MDX files. **NEW (content, small)**

Everything else (titles, metrics, tech, impact, dates, categories) is **HAVE**.
